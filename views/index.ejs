<!DOCTYPE html>
<html>
<head>
	<title><%= title %></title>
	<link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
	<h1><%= title %></h1>
	<p>Welcome to <%= title %></p>

	<h2>Loop</h2>

	<ul>
		<% if (!size) { %>

		<li>jjjjjj - <%= urlz %></li>

		<% } else { %>

		<% urlz.forEach(function(url) { %>
		<li><%= title %> - <%= url %></li>
		<% }); %>

		<% } %>


	</ul>
</body>
</html>
